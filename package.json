{
  "name": "http-middleware",
  "version": "1.0.0",
  "description": "A small middleware written in javascript using the express framework. This allows for someone to make requests to an http resource without exposing the requester to the endpoints bad security posture",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "node",
    "http"
  ],
  "author": "eric-at-zd",
  "license": "MIT",
  "engines": {
    "node": ">=10.16.0",
    "npm": ">=6.0.0"
  },
  "devDependencies": {
    "axios": "^0.26.0",
    "express": "^4.17.3"
  }
}
